

// import react from 

// create a new component - which produces some HTML

const App = function() {
    return <div>hello hello, good people</div>
}

// take component-generated HTML, and display it
// on the page(in the DOM)